Pentru interfata grafica (GUI) vom folosi biblioteca GTK+. (GTK 3.0)

Instalare:

1. sudo apt-get install libgtk-3-dev
Optional: Copiere fisier c_cpp_properties.json in propriul .vscode sau copiere atribute din includePath
