Pentru interfata grafica (GUI) vom folosi biblioteca GTK+.

Instalare:

1. sudo apt-get install libgtk-3-dev
2. Copiere fisier c_cpp_properties.json in propriul .vscode sau copiere atribute din includePath
